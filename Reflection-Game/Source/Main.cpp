#include <windows.h>

int WINAPI WinMain(HINSTANCE instance,
		HINSTANCE previous,
		LPSTR arguments,
		int code) {

	// Window and engine init
	bool isRunning = true;

	// Message que and game loop
	while (isRunning){
		// PostMessage
		// DispatchMessage
		// GetMessage
		// PeekMessage
	}

	return 0;
}